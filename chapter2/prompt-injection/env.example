# OpenAI 配置（本实验仅使用 OpenAI 官方接口）
OPENAI_API_KEY=your_openai_api_key_here

# 模型（默认 gpt-4o-mini，便宜且够用）
OPENAI_MODEL=gpt-4o-mini

# 可选：自定义 API base_url（默认官方；请勿指向已失效的第三方网关）
# OPENAI_BASE_URL=

# 可选：每个 攻击x防御 组合的试验次数（建议 3-5，控制成本）
TRIALS=4
